# Dependencies for the deployed Streamlit app (app.py).
# Streamlit Community Cloud installs from this file. The event-driven stack
# (Inngest/FastAPI/uvicorn) is intentionally excluded — it isn't used by app.py.
streamlit>=1.53.1
openai>=2.15.0
qdrant-client>=1.16.2
llama-index-core>=0.14.13
llama-index-readers-file>=0.5.6
python-dotenv>=1.2.1
